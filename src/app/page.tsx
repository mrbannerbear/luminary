import Banner from "@/ui/components/Banner";

const page = () => {
    return (
        <div className="min-h-screen">
            <Banner/>
            hello
        </div>
    );
};

export default page;